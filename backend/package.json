{
  "name": "stucoxsheets",
  "description": "lksdfj",
  "dependencies": {
    "googleapis": "^58.0.0"
  },
  "scripts": {
    "start": "cd dist/ && functions-framework --target=run",
    "compile": "tsc -p . && node dist/test.js",
    "watch": "tsc -w"
    
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.6.0"
  }
}
